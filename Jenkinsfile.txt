pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh  'echo "Hellow Wordld"'
                sh  '''
                    ech "Multiline shell steps works too"
                    ls -lah
                '''
            }
        }
    }
}